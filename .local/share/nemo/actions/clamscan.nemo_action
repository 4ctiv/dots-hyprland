[Nemo Action]
Name=Clam Scan
Comment=Clam Scan

Exec=clamtk '%F' || kitty --title "clamscan" sh -c 'mkdir -p ~/.clamtk/viruses && clamscan -r --infected --move="${HOME:?}/.clamtk/viruses" %F | less'

Icon-Name=clamtk

Selection=Any
Comment=Scan file/folder using clam antivirus
Extensions=any;
